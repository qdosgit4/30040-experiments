#!/usr/bin/env python3

import shutil
import os

original_file = "py_ex_3_gpuL.slurm"


##  Uniform distribution loop.

for i in range(1, 20):

    ##  Training epochs loop.
    
for i in range(1, 20):

    new_filename = f"file_{i}.txt"
    
    # Copy the file
    shutil.copy(original_file, new_filename)
    
    # Read the copied file
    with open(new_filename, 'r') as f:
        content = f.read()
    
    # Edit the string (replace placeholder with iterator value)
    # Example: replace "PLACEHOLDER" with the current number
    edited_content = content.replace("PLACEHOLDER", str(i))
    
    # Write back to the file
    with open(new_filename, 'w') as f:
        f.write(edited_content)
    
    print(f"Created and edited {new_filename}")
